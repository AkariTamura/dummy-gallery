# Frontend environment example for Vite
# Copy to .env.local and adjust for your environment (do NOT commit secrets)

# Base URL for backend API (include protocol for local development)
VITE_API_BASE=http://localhost:8000

# Application environment (development / production)
APP_ENV=development

# Note: Do NOT place secrets (like ADMIN_PASSWORD_HASH) in frontend env files.
# Set ADMIN_PASSWORD_HASH on the server environment for backend PHP.
